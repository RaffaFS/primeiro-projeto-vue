import { createApp } from 'vue'
import App from './App.vue'

import 'bootstrap/dist/css/bootstrap.min.css'

// fizemos a instalação do bootstrap via terminal e com o import acima
// eu importo um arquivo específico dessa biblioteca de estilo presente
// agora dentro de node_modules

createApp(App).mount('#app')